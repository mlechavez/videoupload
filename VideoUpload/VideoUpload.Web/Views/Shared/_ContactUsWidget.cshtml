@using VideoUpload.Core.Entities

@if (ViewBag.BranchList.Count > 0)
{
    var branches = (IEnumerable<Branch>)ViewBag.BranchList;

    <div class="dropdown dropdown-contact">
        <button class="dropdown-toggle btn btn-contact btn-contact-block" data-toggle="dropdown">
            <i class="fa fa-list fa-lg"></i> Contact Us
        </button>
        <ul class="dropdown-menu dropdown-contact-menu">
            @foreach (var branch in branches)
            {
                <li>
                    <a href="@Url.Action("contact", "home", new { branchName = branch.BranchName })#@branch.BranchName.ToLower()">
                        <i class="fa fa-angle-down fa-lg fa-angle-down-padright"></i> @branch.Name
                    </a>
                </li>
                <li role="separator" class="divider"></li>
            }            
        </ul>
    </div>
} 

<a href="@ViewBag.VideoUrl">
    <i class="fa fa-arrow-circle-o-left"></i> Go back to your porsche video
</a>
